// Settings page styling
const SettingsStyle = {
    // Root (Paper component) styling
    root: {
        padding: 25,
        margin: '1em 0'
    },

    // Header margin
    header: {
        margin: '.1em 0'
    },

    // Divider margin
    divider: {
        margin: '1.5em 0'
    },

    // Add icon (in button)
    addIcon: {
        paddingRight: 5
    },

    // Award inputs
    awardInput: {
        width: '100%',
        margin: '.5em 0'
    },

    // Award container
    awardContainer: {
        marginTop: '1.1em'
    },

    // List Item for Badge Selecter
    badgeListItem: {
        padding: '1em'
    },

    // List text for badge
    badgeListText: {
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center'
    },

    // ListItemText font Styling
    dropDownStyle: {
        fontSize: 16
    },

    //Styling for award icon Button
    awardButton: {
        paddingRight: '0.5em'
    }
};

export default SettingsStyle;
